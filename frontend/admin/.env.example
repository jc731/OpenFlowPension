# Keycloak OIDC — leave blank in development (backend dev bypass applies, no login required)
# Set these to enable the Keycloak login flow and JWT injection.
# VITE_KEYCLOAK_URL=http://localhost:8080
# VITE_KEYCLOAK_REALM=openflow
# VITE_KEYCLOAK_CLIENT_ID=openflow-admin
